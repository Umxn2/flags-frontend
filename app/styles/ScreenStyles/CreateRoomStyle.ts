import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  row: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 20,
  },
  badge: {
    height: 40,
    justifyContent: 'center',
    marginRight: 12,
  },
  footer: {
    marginTop: 'auto',
    width: '100%',
    padding: 20,
  },
  createButton: {
    width: '100%',
  },
});
